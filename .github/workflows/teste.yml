name: Deploy1

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: conection_server
    steps:
      - name: deploy
        env:
          HOST: ${{ conection_server.HOST }}
          PORT: ${{ conection_server.PORT }}
          USER: ${{ conection_server.USER }}
        run:
          echo $HOST $PORT $USER
        
      
