name: Deploy1

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: conection_server
    steps:
      - name: deploy
        env:
          HOST: asdsa #${{ secrets.conection_server.HOST }}
          PORT: ${{ secrets.conection_server.PORT }}
          USER: ${{ secrets.KEY }}
        run:
          echo $HOST $PORT $USER
        
      
